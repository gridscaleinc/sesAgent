# SES Agent Desktop 密钥丢失与离线恢复手册

<!-- ses-current-state package=0.1.0 schema=46 -->

> 文档版本：v1.1
> 更新日期：2026-08-18
> 适用范围：macOS 试点版；Windows 版发布前必须完成同等 DPAPI 演练

## 1. 适用场景

出现以下任一情况时使用本手册：

- 应用显示“ローカルデータを開けません”。
- macOS Keychain 被重置、迁移失败或当前登录用户无法解密应用主密钥。
- SQLCipher 数据库损坏或活动数据库与受保护主密钥不匹配。
- 更换 Mac 后需要从 `.ses-recovery` 包恢复。

恢复专用界面不读取业务数据库，不访问 Gmail 或云端 AI，也不尝试绕过加密。它只允许本地验证恢复包、显示恢复范围并在用户明确确认后安排重启恢复。

## 2. 立即停止的操作

在确认恢复路径前，不要执行：

1. 不删除 `~/Library/Application Support/SES Agent Desktop` 或其中任何文件。
2. 不清理、重置或手工编辑 macOS Keychain。
3. 不重新安装后覆盖原应用数据目录。
4. 不把 `.ses-recovery`、恢复密码、Keychain 截图或业务数据上传到聊天、工单或云盘。
5. 不接受“关闭加密”“修改数据库 Header”或猜测密钥等绕过方案。

如需交给 IT 调查，先在受控加密介质中保留整个应用数据目录的只读副本；副本仍属于高敏数据。

## 3. 有恢复包时

准备：

- 最近的 `.ses-recovery` 文件。
- 创建该包时设置的独立恢复密码（至少 12 字符）。
- 足够的本机磁盘空间。

操作：

1. 启动 SES Agent Desktop，确认进入 `OFFLINE RECOVERY` 页面。
2. 确认页面显示“元データは未変更”和“ネットワーク送信なし”。
3. 输入恢复密码，点击“パッケージを選択して検証”。
4. 选择 `.ses-recovery`。应用先验证整包 AES-GCM，再验证 Manifest、SQLCipher 数据库、文件哈希和文件库 AEAD；验证失败不会替换活动数据。
5. 核对创建时间、Schema、保护数据量和文件数。
6. 输入日文确认词 `復元`，点击“確認して復元”。
7. 应用重启后执行原子切换；旧数据与旧密钥先进入同次恢复的临时回滚点。新数据验证成功后才清除回滚点，失败会恢复原状态。

恢复成功后：

- 重新连接 Google Workspace；OAuth Token 不在恢复包内。
- 重新设置 Cloud Provider 凭据；API Key 不在恢复包内。
- 检查候选人、案件、任务、提案和受管文件数量。
- 创建一个新的恢复包，并把旧包按公司高敏介质策略保管或销毁。
- 确认应用外导出的 ZIP/PDF 不属于恢复范围。

## 4. 没有恢复包时

如果受保护主密钥确实丢失且没有可用恢复包，应用无法通过重新签发或猜测密钥打开原 SQLCipher 数据库。这是加密边界的预期行为，不是可绕过的产品限制。

此时：

1. 保留应用数据目录和设备，不做清理。
2. 检查企业备份、受管介质、旧 Mac 以及负责人保存的恢复包。
3. 由 IT 确认是否只是暂时的 Keychain 权限/登录会话问题；修复后使用页面中的“Keychain を修復した後に再試行”。
4. 若确认主密钥永久丢失且无恢复包，把事件登记为不可恢复数据事故；不要通过删除原目录伪装成已恢复。

## 5. IT 管理员取证清单

只记录非敏感证据：

- 应用版本、macOS 版本、芯片架构。
- 故障发生时间和是否刚完成系统/用户迁移。
- 页面状态是否为 `OFFLINE RECOVERY`。
- 是否存在恢复包、包创建日期和文件大小；不要记录路径、密码或内容。
- 验证失败阶段和稳定错误码；不要复制业务正文。
- 恢复后的 Schema、对象计数、恢复事件时间和 Google Workspace 重新授权状态。

普通日志不得包含恢复密码、主密钥、候选人姓名、电话、地址、邮件正文、文件明文路径或包内容。

## 6. 发布与演练要求

每个签名试点版本至少执行一次真人演练：

1. 用脱敏 Fixture 创建恢复包。
2. 模拟活动主密钥不可用，确认应用进入受限恢复模式而不是退出。
3. 用错误密码和篡改包分别确认失败关闭，活动数据不变。
4. 用正确包恢复，确认任务、候选人/案件、文件库和修订触发器一致。
5. 强制中断 `ready`、`active-moved`、`installed` 三个阶段，确认回滚或继续恢复行为。
6. 验证恢复后 Google Workspace 必须重新授权，云端凭据没有随包迁移。
7. 记录执行人、时间、版本、结果和缺陷编号，不记录个人数据。

当前自动化测试已经覆盖加密包认证、错误/篡改拒绝、不可访问活动数据替换、启动前切换、回滚合同、恢复事件和文件 AEAD。Developer ID 签名版上的真人演练仍是发布门槛。
