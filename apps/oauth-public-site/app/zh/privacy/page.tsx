import { LegalPage, metadataFor } from '../../site-pages'
export const metadata = metadataFor('zh', 'privacy')
export default function ChinesePrivacyPage() { return <LegalPage locale="zh" page="privacy" /> }
