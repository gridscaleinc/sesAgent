import { LegalPage, metadataFor } from '../../site-pages'
export const metadata = metadataFor('zh', 'terms')
export default function ChineseTermsPage() { return <LegalPage locale="zh" page="terms" /> }
