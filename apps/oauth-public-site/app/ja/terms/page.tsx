import { LegalPage, metadataFor } from '../../site-pages'
export const metadata = metadataFor('ja', 'terms')
export default function JapaneseTermsPage() { return <LegalPage locale="ja" page="terms" /> }
