# SES Agent 案件配信 V1 方案

<!-- ses-current-state package=0.1.0 schema=44 -->

## v0.2 变更说明（2026-09-01）

- 产品决策：**是否真的发到了微信群，是 HR 自己的事，系统不再记录**。应用只写下自己确实观测到的事实——「复制」这一动作。
- 功能收敛为：收集案件 → 整理 → 按模板生成中日双语文案 → 一键复制，复制事件自动入账。「已発」按钮、发送台账与"待发/已发"状态全部下线。
- 数据：Schema v43 新增追加型 `case_broadcast_copies`（随案件级联删除）。v42 的 `case_broadcasts` 保留为只读历史，`sales_groups` 转为休眠表，不做破坏性迁移。
- 营业群概念从产品界面完全移除：设置里的营业群编辑器、群推荐、目标群勾选、对话卡片的群标签，以及 `job-case.broadcast.record.local` 工具一并删除。
- 队列状态改为 `新着（未コピー）/ コピー済み / 要補完`；更新通知的基线取「曾经复制过的最高案件版本」，旧台账行同样计入，老设备数据不丢。
- 云端叙述层同步收敛：投影不再包含目标群数量，指令明确禁止模型宣称任何案件"已发送"。
- 案件配信文可通过 `mailto:` 交给系统默认邮件客户端：只预填标题和正文，不指定或保存收件人，不改变复制状态，也不声称邮件已发送；最终发送仍由 HR 在邮件客户端确认。
- 下文 v0.1 的营业群登记、分发推荐、发送台账等章节仅作历史记录保留，不再代表当前实现。

> 版本：v0.1  
> 日期：2026-08-27  
> 状态：设计稿，待 HR 确认（§14 的问题清单回答后进入实施）  
> 当前基线：SES Agent Desktop `feature/v1.0.0` —— Gmail 手动同步（只读）、微信可视读取与粘贴导入、案件导入即生效（有效 / 无效 / 待补充）、案件 18 个结构化字段与字段别名、外发动作审批箱（提案导出已在用）、本地 DLP 出网门  
> 核心决策：系统负责**收集、去重、成文和安全交接**，人负责**确认与发送**。V1 不向微信或邮箱自动发送任何内容；当前可把已通过本地 DLP 的紹介文预填到默认邮件客户端。紹介文由本地模板生成，字段可见性由模板控制，任何禁止外露的字段不进文案。

## 0. 业务现状与目标

HR 的高频日常：案件从 Gmail 或微信群到达 → 逐封阅读、判断是否案件 → 手写一段案件描述 → 挑对应的营业群 → 复制粘贴发送。每天重复几十次，容易漏、容易把内部信息（エンド名、商流、担当者）带出去。

目标：把 HR 的动作压缩为**「看队列 → 确认文案与目标群 → 粘贴」**。

| 步骤 | 现在 | V1 之后 |
|---|---|---|
| 收件 | 逐封看 Gmail、翻微信群 | Gmail 定时同步 + 微信读取/粘贴，统一进「今日队列」 |
| 判断与去重 | 人工 | 已有：案件/人员分类、指纹去重、多来源合并 |
| 案件描述 | 手写 | 模板自动生成紹介文，可改 |
| 挑群 | 凭记忆 | 按规则推荐目标群，HR 确认 |
| 发送 | 复制粘贴 | 一键复制（微信）；邮件走审批（V2） |
| 记录 | 无 | 台账：谁、何时、发到哪个群、发的哪个版本 |

## 1. 范围

### 1.1 包含
- 今日队列：新案件 / 待发 / 已发 / 已失效 / 待补充，按来源筛选，批处理。
- 紹介文模板：HR 可编辑的行模板 + 字段可见性（公开 / 内部 / 禁止）。
- 紹介文生成：本地模板拼装为准；云端润色可选（脱敏投影、逐字校验、不得新增事实）。
- 营业群登记与分发推荐：群属性 + 规则，推荐前 3 个群。
- 一键复制（单条 / 批量依次）与发送台账。
- 更新通知与募集终了通知文案。
- Gmail 定时同步（仅在应用运行时，只读）。
- 对话接入：本地工具生成文案、记录已发。

### 1.2 明确不包含
- 向微信自动发送（没有官方 API，封号与误发风险，外发不可撤回）。
- V1 自动发邮件（V2 走 Gmail 草稿 + 审批箱）。
- 回应跟踪与效果统计（V3）。
- 案件字段抽取本身的改进（沿用现有导入链路）。

## 2. 用户操作流程

### 2.1 主链路（每天 1–2 次）
1. 打开「案件配信」页。队列顶部显示：今日新增 N、待发 N、已发 N、待补充 N。
2. 左侧逐条点开新案件；右侧显示结构化字段、生成好的紹介文、推荐目标群（可勾选/改）。
3. 需要改文案就在文本框里改；改字段则回到案件详情（导入即生效已支持直接编辑）。
4. 点「复制」→ 文案进剪贴板，台账记 `copied`；到微信群粘贴发送后回来点「已发」→ `sent`（可在设置里改为"复制即视为已发"）。
5. 批处理：勾选多条 → 「依次复制」→ 系统按顺序把每条文案放进剪贴板，HR 每粘贴一次点一次「下一条」。
6. 截止时间前 30 分钟，队列里仍有「待发」时提醒。

### 2.2 异常路径
- 不是案件（人员消息、闲聊、广告）：标记「忽略」，不再出现；已有分类器把大部分挡在导入前。
- 字段不足（缺案件名、含识别符）：进「待补充」，右侧补齐后保存即生效，再回到队列。
- 文案中残留禁止字段或识别符占位：复制按钮禁用，标红提示，必须先删除。
- 同一案件再次来信（単価変更、募集終了）：队列显示「更新」徽标，生成更新 / 终了通知文案，目标群默认为上次发送过的群。

## 3. 数据模型（新增，均在本地加密库）

```text
broadcast_templates
  id, name, channel (wechat|email), line_template (多行), field_visibility_json
  (字段 → public|internal|forbidden), footer, revision, updated_at

sales_groups
  id, name, channel (wechat|email), rules_json
  (skill_tags[], regions[], rate_min, rate_max, partner_allow[], partner_deny[]),
  default_template_id, status (active|archived), updated_at

case_broadcasts   （追加式，不允许更新）
  id, job_case_id, job_case_version, group_id, template_id, template_revision,
  kind (new|update|close), text (加密库内保存，供"我发了什么"回看), text_sha256,
  action (copied|marked_sent|drafted|sent), actor_id, created_at

broadcast_settings （单例）
  gmail_sync_interval_minutes (默认 15), daily_deadline (HH:mm), copy_marks_sent (bool)

group_recommendation_feedback （简单计数，不做模型）
  group_id, skill_tag, accepted_count, overridden_count
```

队列不是表，由「有效案件 × 配信记录」派生：无记录 → 新案件；有 `sent` → 已发；案件无效 → 已失效；待补充来自现有审核状态。

删除案件时配信记录随之删除（现有受控删除的影响预览需要增加「配信记录 N 条」）。

## 4. 紹介文生成规则

### 4.1 模板语法
每行 `ラベル：{{field_key}}`；引用为空的行自动省略；支持固定文本行与尾注。例：

```text
【案件】{{title}}
スキル：{{required_skills}}
尚可：{{preferred_skills}}
期間：{{start_date}}
場所：{{location}}／{{remote}}
単価：{{rate_public}}
日本語：{{japanese_level}}
面談：{{interview}}
人数：{{headcount}}
備考：{{notes_public}}
```

- `{{rate_public}}`：公开单价口径，由模板决定：原值 / 上限 / 「応相談」。
- `{{notes_public}}`：備考里去掉内部标记行（以 `※内部` 开头的行不出）。
- 微信版建议 ≤ 500 字；超长时截断尾注前的行并提示。

### 4.2 字段可见性（默认值，HR 可改）

| 档 | 字段 | 行为 |
|---|---|---|
| 公开 | title, role, industry, required_skills, preferred_skills, start_date, working_hours, location, remote, japanese_level, interview, headcount | 进文案 |
| 内部 | rate（默认改用 rate_public）, settlement, contract_chain, payment_terms, work_authorization, notes | 不进文案，除非模板显式引用其公开口径 |
| 禁止 | 任何直接识别符占位（<PERSON_NAME_n> 等）、エンド客户名（来源邮件署名/正文中识别到的公司名，V1 靠 DLP 占位 + HR 确认） | 出现即阻止复制 |

### 4.3 云端润色（可选，默认关）
- 输入：本地生成的文案（已按可见性过滤）的脱敏投影。
- 约束：只允许改措辞与顺序，不得新增数字、技能、地点等事实；输出逐字校验（每个技能/数字必须出现在输入中）；失败回退本地文案。
- 与现有云端车道共用 DLP 门、审计与费用记录。

### 4.4 更新与终了
- 更新：比对新旧版本字段差异，生成 `【更新】{{title}}：単価 60万→65万 / 開始 9月→10月` 形式；只列变化项。
- 终了：`【募集終了】{{title}}`；案件设为无效时自动生成，发往上次的群。

## 5. 营业群与分发推荐

- 群属性：名称、渠道、技术标签、地区、单价区间、来源合作方白/黑名单、默认模板。
- 推荐打分：技术标签重叠数 ×3 + 地区命中 ×2 + 单价区间命中 ×1 + 合作方白名单 ×2；黑名单直接排除；取前 3；无命中时给「默认群」。
- 反馈：HR 修改推荐时记录 `overridden`，接受时记录 `accepted`；同技术标签下 accepted 多的群优先（计数，不做模型）。
- 一个案件可发多个群，每个群一条台账。

## 6. 发送与台账

- 微信：`复制` → 剪贴板 → 台账 `copied`；`已发` → `sent`。批量依次复制同上。
- 邮件（V2）：生成 Gmail 草稿（需要把 Gmail 方法白名单从只读扩展到 `drafts.create`，仍不开放 `send`；发送在 Gmail 里由人点，或 V2.1 经审批箱调用 `send`）。
- 出网门：复制前对文案跑与云端出网相同的本地 DLP 检查；有残留识别符即阻止。
- 台账追加式，不允许改；界面上显示每条案件的发送历史（群、时间、版本、操作人）。

## 7. 队列与看板

- 计数：今日新增 / 待发 / 已发 / 待补充 / 已失效。
- 筛选：来源（Gmail / 微信 / 手动）、群、状态。
- 排序：新案件按到达时间；待发优先。
- 提醒：截止时间前 30 分钟仍有待发 → 应用内通知；当天结束生成「今日配信摘要」（数字 + 未发列表）。

## 8. Gmail 定时同步

- 现状：`syncGoogleWorkspace` 手动触发，只读 scope。
- 新增：定时器（默认 15 分钟，可配 5–60），仅在应用运行且已连接时；失败指数退避；结果进队列并在页面提示「同步于 hh:mm，新增 N」。
- 不改变现有分类、去重、脱敏与落库逻辑。

## 9. 微信

- 可视读取（无障碍树 / 屏幕 OCR）与粘贴导入照旧；群聊一次多案件走现有多记录分段。
- 建议 HR 每天固定两次读取群聊；读取结果直接进队列。

## 10. Agent 对话接入

- 本地工具（写工具，走 ActionRun）：
  - `job-case.broadcast.draft.local`：为指定案件（或今日队列）生成紹介文，返回文案卡片；
  - `job-case.broadcast.record.local`：记录 `copied / sent`。
- 对话示例：「把今天的新案件整理成群消息」→ 返回队列摘要 + 每条文案卡片（复制按钮）；「这条发给关西群了」→ 记台账。
- 云端规划器只看脱敏投影，不看文案原文；文案由本地生成。

## 11. 安全与审计

- 不自动向微信发送；邮件发送必须经审批箱。
- 文案出网前 DLP；模板可见性默认保守（内部字段默认不出）。
- 台账追加式；配信记录随案件受控删除一起删除，删除预览计入。
- 云端润色沿用现有隐私门与费用审计。

## 12. 界面

- 新页面「案件配信」：左列队列（状态 chip、来源图标、到达时间），右侧案件 + 紹介文编辑器 + 目标群勾选 + 「复制 / 已发 / 忽略」。
- 设置新增两页：「紹介文模板」（行模板 + 字段可见性开关 + 预览）、「营业群」（属性 + 规则）。
- 案件详情页增加「发送历史」区块与「生成更新通知」按钮。
- 会话卡片：文案卡片（复制按钮 + 目标群）。

## 13. 分阶段与工期

| 阶段 | 内容 | 工期 |
|---|---|---|
| Phase 1 | 模板与可见性、紹介文生成、队列页、复制与台账、Gmail 定时同步 | 5–7 天 |
| Phase 2 | 群规则推荐与反馈计数、更新/终了通知、Gmail 草稿（白名单扩展 + 审批）、对话工具 | 3–5 天 |
| Phase 3 | 回应跟踪、效果统计 | 视需求 |

## 14. 需要 HR 确认的事项（附默认假设）

1. 紹介文的固定格式：请提供 3 份现在手写的样本。默认按 §4.1。
2. 哪些信息绝对不能进群：默认 エンド客户名、担当者、商流、支払条件不出；单价是否公开、以什么口径公开？默认「応相談」。
3. 营业群划分依据：技术领域 / 地区 / 合作方？一个案件通常发几个群？默认技术领域 + 地区，推荐 3 个。
4. 每天处理量与截止时间：默认早 10:00 前发完，提醒 9:30。
5. 一封邮件多案件、一个案件多封更新，是常态还是例外？默认都支持，更新走差异通知。
6. 微信以群聊为主还是私聊为主？可视读取是否覆盖他们的用法？默认群聊。
7. 「复制即视为已发」还是要求点「已发」？默认要求点「已发」，可切换。

## 15. 风险

- 微信合规：不自动化发送，人手保留在最后一步。
- 内部信息外泄：可见性默认保守，复制前 DLP；模板改动记版本。
- 多来源合并错误：指纹 + 人工合并，误合并可拆分。
- 定时同步：仅前台运行时，间隔可调，失败退避。
- 文案质量：本地模板为准，云端润色可关。
