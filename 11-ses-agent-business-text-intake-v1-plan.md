# SES Agent 业务文本接收 V1 方案

<!-- ses-current-state package=0.1.0 schema=39 -->

> 版本：v0.3  
> 日期：2026-08-25  
> 状态：已按本方案实施于 `feature/v1.0.0`（本地 gate、五路由分类器、案件与人员导入链路、全路径不落原文、离线输入框、4,000 字提示、定向解析修正、脱敏云端分段车道及其自动化测试均已完成；macOS 安装包手工冒烟与本机真实微信样本对照仍待人工执行）  
> 当前基线：SES Agent Desktop `feature/v1.0.0` 现有案件、候选人、Review Center 与本地文件保险库能力  
> 核心决策：HR 粘贴微信业务消息；Main 在任何云端调用之前本地确定性路由；本地规则明确命中时直接执行本地 Tool；本地无法定型的结构化文本走脱敏云端分段车道（云端只见脱敏行投影，返回记录类型、行号范围与逐字核验的字段值；本机在原文分段上还原占位符并生成草稿）；任何导入结果路径都不把原文写入会话；HR 确认后才进入正式数据

## 0. v0.2 修订说明

v0.2 基于对 `feature/v1.0.0` 代码的逐行核对，修正了 v0.1 的两个阻断性设计错误，并重写了分类规则。与 v0.1 的差异：

1. **主链路重构（阻断项）**。v0.1 让粘贴文本走正常 Agent 回合，由 Cloud Planner 选择 `import_business_text {}`。实际代码中，规划投影包含完整用户消息（`agent-cloud-narrative.ts` 的 `userRequest`），出网前必须通过本地 NER + DLP 门，失败即整回合抛错。满是姓名、联系方式、单价的人员消息恰恰是最容易被 DLP 拦截的输入，导致功能主路径频繁不可用，且导入依赖云端可用性。v0.2 改为 **Main 在云端规划之前运行本地 intake gate，确定性路由**；V1 不把导入 Tool 暴露给 Cloud Planner，删除 `Planner name: import_business_text {}`。
2. **会话原文生命周期重定义（阻断项）**。v0.1 只要求"导入成功后持久化脱敏摘要"。实际代码中 `LocalAgentUseCase` 的 `userMessage()` 与 `saveDirectAnswer()` 在成功、失败、DLP 拦截、Cloud 不可用等所有路径都会把原始 `input.message` 写入会话，后续回合的规划投影又会从历史中重新取出这些内容。v0.2 改为**任何被 intake gate 拦截的消息，无论结果如何，都不得把原文写入 `ai_conversations`**；安全摘要在第一次持久化时生成。
3. **分类规则从关键词表改为记录结构规则**。v0.1 把性别、年龄、国籍、日语列为人员强特征，但它们在真实案件消息中作为要求条件极其常见（「外国籍不可」「日本語N2以上」「年齢50歳まで」）。v0.2 改为记录根标签 + 区块内字段的层级规则，并重新定义 `multiple` 为"多个完整记录根"。
4. **新增 `not-intake` 与 `ambiguous-sensitive` 路由**，明确"退回普通 Agent 对话"是显式结果而不是模糊阈值的隐式回落；fail-closed 边界明确**排除裸姓名触发**，避免误伤日常查询与预约主流程。
5. **ambiguous 澄清方式定为显式标注整段重发**（首行【案件】/【人员】），不扫描会话历史、不新增 pending 状态；标注同时作为首次提交的确定性意图信号，取代 v0.1 的自由文本意图检测；ambiguous/multiple 时原文回填输入框（仅内存）。
6. 补充：`txt` 来源的契约拆分、去重幂等生命周期表、4,000 字不对称兜底说明、离线最小界面范围（标记为可裁项）、工期从 4～7 天修正为 7～10 天。

业务终点不变：仍然是"一次一条、一个本地 Tool、一个待审核草稿"。改变的只是进入这个终点的技术路径。

### v0.3 增量（2026-08-25，基于真实样本验收）

首条真实微信样本（月度多案件广播）暴露两件事：编号案件根（`案件1️⃣：`）是规则缺口（已补，计入 `multiple` 判定）；格式长尾确实存在，纯规则会持续追赶。v0.3 据此启用**脱敏云端分段车道**，替代 v0.2 §15 中"云端提取"的观望态度：

- 本地规则明确命中时行为不变（零云端、离线可用）。
- 路由为 `ambiguous-sensitive` 或 `multiple` 时，先尝试云端车道：对原文行编号 → 经现有本地 NER + DLP 脱敏 → 云端模型返回受约束 JSON（每条记录的类型 + 行号范围，1～10 条，严格校验）→ Main 在**本机原文**的对应分段上运行既有本地提取器，逐段建 ActionRun 与待审核草稿。
- 云端看不到未脱敏原文。v0.4 起它可产出字段值，但每个值必须逐字来自该记录自己的脱敏行（整值或以「、」相连的每个片段），否则丢弃；占位符在本机还原。分错、抽错都由人工审核兜底。
- 车道任何失败（DLP 拦截、云端不可用、协议违规、取消、全部分段导入失败）都回落到 v0.2 的本地提示路径（标注重发 / 拆分），不留中间状态。
- 多记录广播由此获得受控批量导入：一条消息 → N 个草稿 → 逐条人工确认；"一次一条"的人工审核纪律不变。
- 独立回滚开关 `SES_BUSINESS_TEXT_CLOUD_ASSIST_ENABLED=0`（只关车道，不动本地 gate）。

## 1. 方案目标

第一版只解决一个明确问题：

> HR 从微信手动复制一条案件信息或一条人员信息到 SES Agent 聊天框后，系统能够正确识别、解析并形成待审核草稿；HR 确认后，数据进入现有案件库或候选人库。

本方案不读取微信，也不与微信建立连接。微信只是业务文本的来源，实际入口是 SES Agent 当前聊天框。

V1 的终点是：

- 单条案件消息可以生成一个 Job Case Review，确认后成为正式案件。
- 单条人员消息可以生成一个 Candidate Review，确认后成为候选人档案。
- 类型不明确、混合了多条记录或同时包含案件与人员时，不写入任何业务数据，并要求 HR 拆分或标注类型后重发。
- 所有正式入库动作继续经过人工审核，不允许模型直接写入正式案件或候选人数据。
- **导入类文本从始至终不进入云端投影，不触发该回合的 Cloud DLP，不依赖云端可用性。**
- **任何导入结果路径都不把原文写入会话持久化。**

## 2. 用户操作流程

### 2.1 主链路

```text
HR 提交聊天消息
  │
  ├─ Main 本地业务文本 Gate（先于任何 Cloud 调用）
  │    │
  │    ├─ 明确单条案件
  │    │    └─ 直接执行 business-text.import.local
  │    │         └─ Job Case Review
  │    │
  │    ├─ 明确单条人员
  │    │    └─ 直接执行 business-text.import.local
  │    │         └─ Candidate Review
  │    │
  │    ├─ 像业务记录，但本地规则无法定型（ambiguous-sensitive / multiple）
  │    │    └─ 脱敏云端分段车道（可独立关闭）
  │    │         ├─ 行编号 → 本地 NER + DLP 脱敏 → 云端返回记录类型+行号范围
  │    │         ├─ Main 在本机原文分段上运行既有提取器 → 逐段草稿 → Review
  │    │         └─ 任何失败 → 回落本地提示（标注重发 / 拆分）；原文回填输入框
  │    │
  │    └─ 明确不是业务记录（not-intake）
  │         └─ 进入现有 Cloud Planner（现有搜索、匹配、预约等能力不变）
  │
  └─ intake 回合的会话只持久化安全摘要，不持久化导入原文
```

1. HR 在微信中复制一条完整的案件消息或人员消息。
2. HR 将文本粘贴到 SES Agent 主聊天框并发送，可选择在首行加显式类型标注（见 2.2）。
3. Main 在任何云端调用之前运行本地 gate，得到五种路由之一（见 §4.1）。
4. 命中 `job-case` 或 `candidate` 时，Main 直接构造受控 Tool 调用并执行，生成一个 Job Case Review 或 Candidate Review，回复安全摘要与 Review Center 导航。
5. HR 在 Review Center 检查、补充或修改字段；确认后草稿才进入正式案件库或候选人库。
6. `not-intake` 的消息进入现有 Cloud Planner 流程，行为与当前完全一致。

### 2.2 显式类型标注

HR 可以在原文首行增加 `【案件】` 或 `【人员】` 标注。标注的判定规则：

- 标注是**确定性解析的意图信号**，在首次提交时即生效，不只是 ambiguous 后的补救手段。它取代 v0.1 中"用户明确表达'把下面案件录入'等自由文本意图作为辅助信号"的设计。
- 标注 token 限定为首行精确匹配，接受双语变体：`【案件】`；`【人员】`、`【人材】`、`【要員】`。V1 不做自由文本意图识别。
- 标注只能解决**类型歧义**：文本具备记录结构但根证据不足时，标注使其按标注类型进入解析。
- 标注**不能覆盖结构判定的矛盾**：首行标了 `【人员】` 但正文命中决定性案件结构时，返回 `ambiguous-sensitive` 并说明矛盾，不默信任何一方。
- 标注**不能覆盖 `multiple`**：即使加了 `【人员】`，文本中有两组明确人员记录，仍要求拆分。

### 2.3 无法自动处理时

系统不猜测、不拆错，也不创建半成品数据：

- `ambiguous-sensitive`（像业务记录但类型不明确）：提示 HR 在原文首行加 `【案件】` 或 `【人员】` 后**整段重新发送**。只回复"这是人员"四个字不会触发导入——原文不在会话中，Tool 作用域严格限当前消息。
- `multiple`（多条或混合）：提示 HR 拆分后逐条提交。
- 以上两种结果都**不保存原文**；Renderer 将原文**回填到输入框**（纯内存，不持久化），HR 加一行标注或删掉多余记录即可重发，不必回微信重新复制。
- 文本超过 4,000 字：按 §9 的不对称兜底提示，不静默截断。

### 2.4 当前能力核对

以下"已有"与"待实施/待改造"必须分开理解（行号为 `feature/v1.0.0` 基线，可能漂移）：

| 能力 | 当前状态 | 代码位置或说明 |
| --- | --- | --- |
| Agent 请求校验与 requestId 幂等检查（gate 插入点之前的既有步骤） | 已有 | `apps/desktop/src/main/agent-ipc.ts` `handleExecute`（约 843 行起） |
| Cloud Planner 规划，投影包含完整 `userRequest`，出网前过本地 NER + DLP，失败抛"已阻止发送" | 已有，intake 路径必须绕开 | `agent-ipc.ts` 约 1014–1022 行；`agent-cloud-narrative.ts` 约 425–439、653–676 行 |
| Main 对 Planner 计划的确定性改写先例（预约重定向，发生在 plan 之后） | 已有，本方案将拦截再前移一层 | `packages/agent/src/index.ts` 约 765–782 行 |
| 会话持久化无条件保存原始用户消息（成功、失败、DLP 拦截路径均如此） | 已有行为，**需要改造** | `packages/agent/src/index.ts` `userMessage()`（约 656 行）、`execute()`（约 786 行）、`saveDirectAnswer()`（约 1190 行） |
| Renderer 发送时原文先放内存 pending 消息，Main 返回后接受持久化快照 | 已有交互基础 | `apps/desktop/src/renderer/components/AgentWorkspace.tsx` 约 1046、1090 行 |
| Renderer 在 `cloudConnected === false` 时隐藏整个聊天输入框 | 已有行为，离线段需要调整 | `AgentWorkspace.tsx` 约 1388 行 |
| 从当前聊天消息创建案件或人员草稿的统一 Tool | 没有，属于本方案 | 需要新增 `business-text.import.local` |
| 聊天粘贴案件生成待审核草稿（专用 IPC，接受 10 万字） | 已有 | `apps/desktop/src/main/ipc/job-cases.ts` 约 145 行；`packages/shared/src/schemas.ts` 约 1571 行 |
| 案件重复提交行为：只加 `BUSINESS_DUPLICATE` 警告，仍新建 source 和 draft | 已有行为，**"返回已有 Review"是待实现的新行为** | `job-cases.ts` 约 158–177 行；查询原语 `findJobCaseReviewByBusinessFingerprint` 已存在 |
| 候选人字段提取和 Candidate Review | 已有 | `packages/resume/src/index.ts` `extractCandidateDraft`（约 1836 行） |
| 候选人来源暂存、解析文档和持久化 | 已有文件链路 | `packages/persistence/src/stores/candidate-store.ts` |
| 将聊天文本作为受信 `txt` 候选人来源 | 没有，属于本方案 | 需要 Main-only 文本暂存与契约拆分，见 §7 |
| Agent 消息长度限制 4,000 字 | 已有 | `packages/shared/src/schemas.ts` 约 1255 行 |
| 简历上传格式契约（五种文件格式，三处） | 已有，需拆分而非放宽 | `packages/shared/src/contracts.ts` 约 82 行；`schemas.ts` 约 165 行；`packages/parsers/src/index.ts` 约 93 行 |

本机真实微信样本的只读核对结论保持不变：案件和人员信息都包含足以结构化的业务标签，现有解析器能提取一部分核心字段；但邮件分类规则会因宽泛的"要件"等词把人员经历误判成案件，案件解析存在跨段落取错字段的问题。V1 不复用邮件分类器，使用 §4 的记录结构规则。

以上仅说明方案具备可复用基础，不代表业务文本接收功能当前已经完成。

## 3. V1 范围

### 3.1 包含

- 使用现有 Agent 聊天框接收 HR 手动粘贴的文本。
- Main 本地 intake gate：五路由确定性分类，先于任何云端调用。
- 命中导入时由 Main 直连执行本地 Tool，整回合零云端调用（plan、stream、streamAnswer 均为零）。
- 案件文本复用现有案件草稿、去重和 Job Case Review 流程。
- 人员文本复用现有候选人提取、Candidate Review、候选人档案和加密源文件流程。
- 对真实微信文本暴露出的少量解析问题做定向修正。
- 任何 intake 结果路径都不把原文写入会话；安全摘要在首次持久化时生成。
- 首行显式类型标注（【案件】/【人员】/【人材】/【要員】）。
- ambiguous/multiple 时原文回填输入框（仅内存）。
- 保留 4,000 字输入限制，界面显示字数与不对称兜底提示。
- 离线最小界面：Cloud 未连接时聊天框仍可用，仅支持本地导入等本地操作（可裁项，见 §11）。
- 对导入动作记录最小审计信息，不在日志、ActionRun 或云端保存原始个人信息。

### 3.2 明确不包含

- 自动读取、监听或同步微信消息。
- 把 `business-text.import.local` 暴露为 Cloud Planner 可选择的 Tool。
- 通过扫描会话历史找回澄清前的原文；可重启的 pending clarification 状态（留给 V1.1，见 §15）。
- 一次导入多个案件、多个候选人，或案件和人员的混合批处理。
- 新建批次表、批处理队列或通用数据导入平台。
- 自动拆分长聊天记录。
- 自动修改案件或候选人状态。
- 候选人模糊去重、自动合并档案；跨删除的哈希墓碑。
- 候选人自动进入可推荐人才池。
- 为该功能新增专用聊天模式、入口页或复杂结果卡片。
- 任何未经本地 DLP 脱敏的云端调用；未经本机逐字核验的云端字段值永远不进入草稿。
- 提高当前 4,000 字输入上限。
- 完整的离线对话能力（离线时普通自然语言问题只返回确定性本地提示）。

## 4. 本地路由与分类规则

### 4.1 路由类型

```ts
type LocalBusinessTextRoute =
  | 'not-intake'
  | 'job-case'
  | 'candidate'
  | 'ambiguous-sensitive'
  | 'multiple'
```

| 路由 | 系统动作 | 写入草稿 | 云端调用 |
| --- | --- | --- | --- |
| `not-intake` | 进入现有 Cloud Planner，行为与当前一致 | 否 | 现有流程（含现有 DLP） |
| `job-case` | Main 直连解析案件并打开 Job Case Review | 是，一个 | 零 |
| `candidate` | Main 直连解析人员并打开 Candidate Review | 是，一个 | 零 |
| `ambiguous-sensitive` | 先走脱敏云端分段车道；失败回落"标注后重发"提示，原文回填输入框 | 车道成功时按分段建草稿 | 仅脱敏行投影 |
| `multiple` | 先走脱敏云端分段车道（受控批量）；失败回落"拆分"提示，原文回填输入框 | 车道成功时逐段各建一份 | 仅脱敏行投影 |

只有 `not-intake` 允许进入 Cloud Planner；其余路由不经过 Planner。云端分段车道是独立于 Planner 的受控调用（见 §5.5），关闭后 `ambiguous-sensitive` / `multiple` 恢复纯本地提示。"退回普通对话"是显式路由结果，不允许由模糊置信度阈值隐式决定。

### 4.2 归一化预处理

标签匹配前统一处理，这些在微信转发的日文业务消息里是常态：

- 全角冒号 `：` 与半角冒号 `:` 等价。
- 标签内的全角空格（如 `氏　名`）归一化。
- 剥离 `■`、`●`、`【】`、`▼`、`★` 等行首装饰前缀。
- 常见标签变体归并（如 `必須スキル` / `必須要件` / `必須`，`尚可` / `歓迎スキル` / `尚可スキル`）。

### 4.3 结构规则层级

分类基于**记录根标签 + 区块内字段**，不是关键词共现。

**决定性案件结构**：至少命中一个案件根标签，并与两个以上案件字段共同出现。

- 高权重根标签：`案件名：`、`案件概要：`、`募集人数：`、`作業内容：` / `業務内容：`
- 编号案件根：`案件1️⃣：`、`案件②：`、`案件2：` 等"案件+编号"形式，以及裸 `案件：`，各计为一个案件记录根（真实微信案件广播的标准写法）
- 案件字段：`必須スキル：`、`尚可スキル：`、`商流：`、`精算：`、`面談回数：`、`単価：`、`勤務地：`、`参画時期：`、`就業形態：`

**决定性人员结构**：至少命中一个身份根标签，并与两项以上人员资料字段共同出现。

- 身份根标签：`氏名：`、`姓名：`、`イニシャル：`、`要員情報`、`人材情報`（及等价人员标题）
- 人员辅助字段：`性別：`、`年齢：`、`国籍：`、`最寄駅：`、`所属：`、`開始日：`、`単金：`、`日本語：`、`希望：`

**辅助字段只有位于已识别的人员区块内时才计入人员证据。** 性别、年龄、国籍、日语单独出现不提供任何人员类型证据——它们在案件消息中作为要求条件极其常见。

**不提供类型证据的自由文本**（可同时出现在案件要求和人员经历中，本身不能用于分类）：

- `要件定義`
- `日本語N2以上`
- `外国籍不可`
- `年齢50歳まで`
- `常駐`
- 单独出现的 `単価`
- 技术名称与项目阶段词汇

### 4.4 `multiple` 的定义

`multiple` 表示**检测到多个完整记录根**，不表示"案件分数和人员分数同时高"：

- 两个 `氏名` / `イニシャル` 人员区块；
- 两个 `案件名` / `案件概要` 案件区块；
- 两条以上编号案件根（`案件1️⃣：`、`案件2️⃣：`…，月度案件广播即属此类）；
- 一个完整案件区块加一个完整人员区块。

一条案件中出现"年龄、国籍、日语要求"，仍然只有一个案件根，不是 `multiple`。

### 4.5 fail-closed 边界

对"像结构化业务记录但判断不准"的输入 fail closed，走 `ambiguous-sensitive`，不能因置信度低回落到云端。触发条件严格限定为：

- **记录结构信号**：多条"标签：值"行，或命中任何根标签，但不足以判定为单条决定性记录；或
- **联系方式级 PII**：消息中出现电话号码或邮箱地址。正常查询极少包含它们，作为 fail-closed 触发是安全的。

**裸姓名不触发 fail-closed。** 姓名在日常查询里无处不在（"田中さんの面接を予約して"、"看一下 A.B 的匹配结果"），这些是现有 planner 驱动的核心功能的典型输入；姓名进入云端投影已由现有本地 NER + DLP 脱敏管道处理。"裸姓名 + 自由文本"必须路由为 `not-intake`，否则 Agent 的日常能力会被这个功能拖垮——该边界必须有正反两向 fixture 保护（见 §13）。

### 4.6 与普通查询的关系

- 普通查询，例如"最近有什么 Java 案件？"，路由为 `not-intake`，继续走现有案件搜索。
- 含姓名的普通指令，例如"明天10点约田中面试"，路由为 `not-intake`，现有预约重定向链路照常工作。

## 5. 执行路径与 Tool 设计

### 5.1 Gate 位置与接管原则

Gate 运行在 `agent-ipc.ts` 的 `handleExecute` 中：**输入 schema 校验和 requestId 幂等检查之后、`narrativeStreamer` 检查和 `plan()` 调用之前**。这不同于既有的预约重定向先例（`packages/agent/src/index.ts` 约 765 行）——那个改写发生在 Planner 调用之后；intake 拦截必须再前移一层，敏感文本才不会进入规划投影。

**接管原则（实现陷阱，必须遵守）**：`handleExecute` 现有的失败兜底路径（DLP 抛错、plan 解析失败、Cloud 不可用）最终都落到 `saveDirectAnswer()`，而它无条件持久化原始 `input.message`。因此一旦 gate 判定为 intake（非 `not-intake`），该回合必须**完整地由 intake 执行器接管，包括它自己的全部错误处理**；任何 throw 都不允许穿透回外层通用 catch。这是"任何结果路径不落原文"原则最容易被破坏的位置。

同样，不能复用 `LocalAgentUseCase.execute(input, syntheticPlan)`，它仍会保存 `input.message`。应增加独立的本地 intake turn 执行入口，接口上明确区分：

- `rawSourceText`：只传给当前本地 Tool，用完即弃；
- `persistedUserContent`：只允许安全摘要。

两者**不得共用同一个字符串类型**。用 branded type 或独立包装类型隔离，让"把原文赋给持久化字段"在编译期就不成立，而不是靠 code review 把关。

### 5.2 Tool 权限边界

只增加一个领域 Tool：

```text
Domain Tool: business-text.import.local
```

V1 **不注册对应的 Planner name**。模型不传入原始文本、类型、字段、文件路径或业务 ID；Main 从当前可信上下文绑定 `conversationId`、`requestId`、`turnId` 与当前 `input.message`。

| 属性 | V1 设置 |
| --- | --- |
| 发起来源 | `user-command`（用户消息经 Main intake gate 确定性命中） |
| Cloud Planner 可见性 | 不可见，V1 不暴露 |
| 作用域 | `current-agent-message`，严格限当前消息 |
| 本地读取 | 允许 |
| 本地写入 | 只允许创建待审核草稿 |
| 文件写入 | 仅人员文本需要加密暂存 |
| 外部读取 | 不允许 |
| 外部写入 | 不允许 |
| 额外云端调用 | 不允许（该回合 plan / stream / streamAnswer 均为零） |
| 幂等方式 | 基于内容指纹，生命周期规则见 §10 |
| 风险级别 | `safe-local` |
| 执行前审批 | 无；正式入库仍需 Review Center 人工确认 |

Tool 仍受 `DomainToolRegistry`、权限策略、ActionRun 和幂等约束管理。Main 内的本地分类器是业务类型的最终判断者。

### 5.3 回合生命周期与界面表现

intake 回合是纯本地回合，Renderer 不得出现云端回合的界面元素：

- 不显示"AI 正在选择 Tool"或规划阶段进度。
- 不显示模型名称。
- 不显示远端取消或计费提示。
- 完成后的助手消息是本地确定性文本：安全摘要 + 处理结果 + 现有 Review Center 导航信息，不新增专用业务结果组件，不走云端叙述。

### 5.4 离线行为

当前 Renderer 在 `cloudConnected === false` 时隐藏整个聊天输入框（`AgentWorkspace.tsx` 约 1388 行）。gate 前移后，导入不再依赖云端，离线可用是自然收益，但需要同步的最小界面调整：

- 未连接 Cloud 时仍显示聊天框。
- 明确提示"离线状态仅支持案件/人员文本导入等本地操作"。
- 离线时 `not-intake` 的消息返回确定性的本地提示（不假装能回答自然语言问题）。

**范围备注**：离线段是排期受压时的首个可裁项——主要收益（DLP 可用性、隐私）在云端连通时已由 gate 兑现。裁剪时必须整段裁掉（聊天框保持现状隐藏），不能只裁一半：若聊天框离线可见，则离线 `not-intake` 的确定性提示不可省略。

### 5.5 脱敏云端分段车道

对所有取入路由（v0.4 起含本地已定型的 `job-case` / `candidate`），Main 先发起一次受控云端调用（`AgentCloudNarrativeService.extractBusinessText`，走既有 `invokeCloud` 管道，`projectionKind: 'business-extraction'`）：

**云端看到什么**：原文按行编号（`L1: …`）序列化为投影，出网前经现有本地 NER + `redactTextForCloud` DLP 门脱敏；DLP 未通过即抛错、本回合回落本地提示。云端永远接触不到未脱敏原文。

**云端返回什么**：受约束 JSON——`{"decision":"records","records":[{"kind":"job-case"|"candidate","startLine":n,"endLine":n,"fields":{...}}]}`（1～10 条、行号升序不重叠、越界即整体拒绝）或 `{"decision":"unusable"}`。`fields` 只允许案件 / 人员的既有字段键，未知键整体拒绝；每个值必须逐字来自该记录自己的脱敏行（整值或以「、」相连的每个片段），不满足的值丢弃、不修复；占位符由本机映射还原，残缺占位符的值丢弃。协议校验是严格的，其余任何违规不修复、直接回落。

**Main 用它做什么**：按行号在**本机原文**上切分，每段经独立的 `business-text.import.local` ActionRun（按回合幂等，回合结束后绑定到该 turn）走既有本地导入（案件指纹去重 / 人员 SHA-256 去重照旧生效）；核验过的云端字段值覆盖同名本地字段（案件值先映射回本次导入的占位符空间），仍为 `needs_review` 并带 `CLOUD_ASSISTED_FIELD_EXTRACTION` 标记，生成待审核草稿。决定性路由（`job-case` / `candidate`）以本地分类为最终判断：云端返回的记录类型与之不一致时整体回落本地导入器。单段结果沿用本地路径的导航块；多段用汇总说明 + Review Center 导航。会话仍只持久化安全摘要。

**失败语义**：DLP 拦截、云端不可用、协议违规、取消、全部分段导入失败——一律回落 v0.2 本地提示路径，原文回填输入框，不留部分状态假象（已成功的分段草稿保留，由汇总文案如实报告成功/失败条数）。

**回滚**：`SES_BUSINESS_TEXT_CLOUD_ASSIST_ENABLED=0` 只关车道；`SES_BUSINESS_TEXT_INTAKE_ENABLED=0` 关整个 gate。

### 5.6 取入后的会话续接（v0.4）

一次粘贴产生的案件草稿，在会话里必须能直接继续处理，而不是只留一张“去审核中心”的卡：

- **`job-case-draft-cards` 块**：取入回合（本地路径与云端车道）为每条成功导入的案件草稿生成一张卡（`DRAFT_n`，粘贴顺序），内容取自持久化后的草稿——本机脱敏后的字段值、缺失字段数、注意项数、审核状态、确认后的正式案件引用。Renderer 用 bootstrap 里的实时 review 覆盖卡片状态：草稿在审核中心确认后，同一张卡直接出现“跑匹配”。reviewId 与案件 id 只是本机路由元数据，云端投影只看到 `DRAFT_n` 标签与业务字段。
- **批次**：每次取入铸造 `intakeBatchId`，写入新草稿的 `draft_json.intakeBatchId`（无迁移；重复命中的既有草稿保留原批次），并作为 `salesAgentState.lastIntakeBatch`（含卡片所在消息 id 与 reviewId 列表）保存。删除草稿时卡片原位墓碑化（无标题、无字段、无路由），批次指针剔除该 reviewId。
- **`job-case.draft.read.local` 工具**（规划目录名 `read_imported_case_drafts`，参数 `draftOrdinal|null`）：只读本会话最近一次取入的草稿，序号按粘贴顺序解析；`null` 读取整批。输出仍是 `job-case-draft-cards` 块 + 批次深链，经既有 DLP 投影进入云端叙述。作用域 `conversation-intake-drafts`，与其他读工具同样按回合幂等、绑定 turn。
- **审核中心批次视图**：`system-access: review-center` 可携带 `intakeBatchId` + `reviewIds`，右侧工作区只列出本次粘贴的草稿，行内“快速确认”以草稿当前字段值原样提交既有 `job-case-review:submit`（主进程规则不变：案件名必填、无直接标识符、revision 一致）。不提供“一键全确认”。
- **确认即匹配**：卡片上已确认且有效的案件提供“跑匹配”，即以该案件为 `selectedJobCaseRef` 发送一条匹配回合；对草稿直接跑匹配仍不做。

## 6. 案件文本处理

### 6.1 复用现有能力

将现有聊天粘贴案件处理逻辑提取为 Main 内可复用服务，由以下两个入口共同调用：

- 现有案件粘贴 IPC；
- `business-text.import.local` Tool。

复用现有能力：本地实体识别和脱敏、业务指纹去重、案件草稿持久化、Job Case Review、HR 确认后的正式案件创建。

### 6.2 定向修正

真实微信样本验证显示，当前案件解析可能把价格行中的"根据技能和经验面议"误当成必须技能。V1 把字段提取限制在对应段落内：

- 必须技能只从"必须技能"标题开始，读取到下一个同级标题为止。
- 尚可技能只从"尚可技能"段落提取。
- 单价和精算分别提取，不把价格说明写入技能字段。
- 无法确定的字段保持为空并进入 `needs_review`，不根据相邻段落猜测。

## 7. 人员文本处理

### 7.1 保持现有候选人链路

人员文本不新增另一套候选人模型。它先转换成现有候选人解析链路可接受的内部文本来源，再复用：

1. Main 将当前文本写入本地加密文件保险库。
2. Main 生成按行组织的 `DocumentIR`。
3. 调用现有 `extractCandidateDraft`。
4. 保存解析文档与候选人待审核草稿。
5. 打开 Candidate Review。
6. HR 确认后生成或更新候选人档案。

为此仅增加 Main 内部可信文本暂存能力：

```ts
fileVault.stageTrustedText(text, generatedName)
```

内部文件名使用内容摘要，不包含候选人姓名：

```text
agent-paste-<hash8>.txt
```

该命名满足 `documentIrSchema` 现有约束（name ≤ 180、size 为正且 ≤ 25MB）。

### 7.2 `txt` 来源的契约拆分

`txt` 不是数据库迁移，但也不是一个局部枚举改动。当前 `SupportedResumeFormat`（`contracts.ts` 约 82 行）、`stagedLocalFileSchema`（`schemas.ts` 约 165 行）和 `documentIrSchema`（`parsers/src/index.ts` 约 93 行）都只接受五种文件格式。**不要把所有地方统一放宽成 `txt`**，而是拆分契约：

```ts
type SupportedResumeUploadFormat =
  | 'pdf'
  | 'docx'
  | 'xlsx'
  | 'xls'
  | 'xlsb'

type CandidateSourceFormat =
  | SupportedResumeUploadFormat
  | 'txt'
```

- Renderer 文件选择和外部附件 IPC 仍只接受原来的五种格式。
- `txt` 仅允许由 Main 的 `stageTrustedText` 创建，不开放用户直接上传任意 `.txt`。
- `DocumentIR`、持久化行和原始文档预览接受 `CandidateSourceFormat`。

SQLite 列本身是 TEXT，不需要 Schema 40；但共享契约、Zod、parser、row mapper、预览和删除测试都要随之修改，实施时不要按零成本估。

### 7.3 定向修正

- 支持中文或日文标签中包含全角空格的写法（归一化规则见 §4.2）。
- 识别 `开始日`、`单金`、`最近车站`、`希望`、`日语` 等常用标签。
- 没有法定姓名时，将首字母或匿名标签作为待审核显示名，不虚构姓名。
- 工作方式优先采用明确的"希望"字段，不因经历描述中出现"常驻"就覆盖远程偏好。
- 只有明确的项目标题或期间结构才能生成项目经历，避免把普通说明拆成虚假项目。

### 7.4 人才池边界

人员文本确认后只创建或更新候选人档案，不自动完成以下动作：

- 不自动标记为可推荐；
- 不自动进入活跃人才池；
- 不自动匹配或提交到案件；
- 不自动联系候选人或客户。

这些动作继续遵守现有人工判断和审核流程。

## 8. 会话持久化与隐私

### 8.1 总原则

> 任何被本地 intake gate 拦截的消息（路由为 `job-case`、`candidate`、`ambiguous-sensitive`、`multiple`），无论结果是成功、重复、解析失败或本地写入失败，都不得把原始文本写入 `ai_conversations`。安全摘要必须在第一次持久化时就生成，不能先保存原文再修改。

这比 v0.1 的"导入成功后改写摘要"更严格，原因：现有代码在成功、失败、DLP 拦截、Cloud 不可用的所有路径都保存原始用户消息；后续每个回合的规划投影又会从最近历史回合中重新取出这些内容（单条历史消息最多投影 1,200 字）。只有从第一次持久化就不落原文，才能同时覆盖成功路径、失败路径和后续回合的云端投影。

不依赖删除时补救：候选人删除虽会清理关联会话引用，但 `ambiguous-sensitive` / `multiple` 的消息没有候选人引用，删除链路管不到它们。唯一正确的做法是从一开始不保存。

### 8.2 显示与持久化行为

| 阶段 | HR 在界面看到什么 | 磁盘中保存什么 |
| --- | --- | --- |
| 点击发送后、处理进行中 | 当前粘贴原文（内存 pending 消息，短暂显示） | 尚未写入会话 |
| 本地分类/导入完成后 | 安全摘要、处理结果与 Review 导航 | 只有安全摘要 |
| `ambiguous-sensitive` / `multiple` | "原文未保存，请标注类型或拆分后重新提交"；原文回填到输入框待修改重发 | 不保存原文 |
| 处理失败 | "本地处理失败，原文未写入会话" | 不保存原文 |
| 打开 Review Center | 可从受控来源查看原文或脱敏原文 | 人员原文位于加密文件保险库；案件沿用脱敏来源 |
| 应用重启后 | 安全摘要 | 安全摘要 |

Renderer 已具备该交互基础：发送时原始内容先放在内存 `pendingMessage` 中，Main 返回正式 conversation 后再接受持久化快照并清除 pending 消息。因此不需要新增复杂卡片，只要 Main 返回的用户消息本身已经是摘要即可；回填输入框同样只使用内存中的原文。

### 8.3 审计与云端边界

- 所有 intake 路由都先走 §5.5 车道：云端只接收脱敏行投影，返回分段信息与字段值；字段值经本机逐字核验与占位符还原后才进入草稿。车道关闭、失败或与决定性本地类型不一致时，回落本地标签解析器（离线可用）。
- `not-intake` 消息走现有 Planner 路径，沿用现有本地 NER + DLP 脱敏管道，行为不变。
- ActionRun 只记录内容摘要（哈希）、路由结果、草稿 ID、处理状态和时间，不记录原始文本。
- 应用日志、错误日志和测试快照不得包含原始人员信息；错误信息不回显原文。
- 人员原文作为加密来源保存，沿用现有候选人删除链路清理文件和映射。
- 案件原文沿用现有脱敏来源保存方式。
- 真实微信聊天记录只用于本机验收，不复制到公开仓库，不进入测试 fixture，不写入本文档。

## 9. 输入长度与不对称兜底

V1 保留 Agent 通道 4,000 字限制（`schemas.ts` 约 1255 行），补齐前端反馈：

- 聊天输入框设置 `maxLength={4000}`，接近上限时显示字数。
- 超限时明确提示，不在浏览器端静默截断。

兜底是**不对称**的，作为已知限制写明，不把"超过 4,000 字"统一描述成"拆分即可"：

- **案件超长**：提示 HR 使用现有案件专用粘贴入口（接受 100,000 字，`schemas.ts` 约 1571 行）。
- **人员纯文本超长**：目前没有等价的超长粘贴入口。如果有 PDF、DOCX 或 Excel 文件，走现有简历导入；否则 V1 只能要求缩减到一条完整记录。

本机真实聊天记录超过 4,000 字是因为包含连续多条业务消息；它是本地验证语料，不是 V1 支持的一次提交格式。

## 10. 去重与幂等生命周期

### 10.1 指纹

- **案件**：继续使用现有业务指纹（基于脱敏后的 subject/body 计算，返回已有 Review 时必须采用相同的归一化）。
- **人员**：对标准化后的完整文本计算 SHA-256，仅本地保存。

V1 不做姓名相似、经历相似或联系方式相似的模糊合并；疑似同一人但文本不同的情况交由 HR 在 Review Center 判断。

### 10.2 重复提交的生命周期规则

注意：当前案件粘贴入口对重复项只加 `BUSINESS_DUPLICATE` 警告并仍然新建 source 和 draft；下表的"返回已有"是**待实现的新行为**，不是复用现状。

| 已有记录状态 | 重复提交结果 |
| --- | --- |
| awaiting-review | 返回并打开已有 Review，不新建草稿或源文件 |
| 已确认、正式记录仍 active | 提示"已经导入"，打开正式记录或已完成 Review；不新建、不复活草稿 |
| 已确认但 archived | 提示"记录已归档"，打开归档记录；不自动恢复 |
| 上一次处理失败且没有完整草稿 | 清理残留（含人员链路的孤儿加密暂存文件）后允许重新执行 |
| 已被用户永久删除 | 不返回、不复活已删除对象；本次显式重新提交视为一次新导入 |

最后一条符合现有删除语义：本地案件和候选人永久删除会移除来源记录，只有 Gmail 来源有防止邮件再次同步的 tombstone。不为跨删除去重偷偷保留新的永久哈希墓碑，除非产品以后明确要求。

## 11. 实施拆分

| 阶段 | 内容 | 估算 |
| --- | --- | --- |
| 一 | 本地 pre-planning gate、五路由分类器、Main 直连 Tool 执行入口、"云端调用为零"测试 | 1.5～2 天 |
| 二 | 隐私安全的会话持久化（全部结果路径）、`rawSourceText`/`persistedUserContent` 类型隔离、Renderer 离线与临时消息/回填行为 | 1.5～2.5 天 |
| 三 | 案件粘贴服务抽取复用、解析段落边界修正、生命周期去重 | 1～1.5 天 |
| 四 | Main-only `stageTrustedText`、`CandidateSourceFormat` 契约拆分、候选人解析修正与删除链路验证 | 2～3 天 |
| 五 | 匿名真实 fixture、重启恢复、删除、隐私检查、安装包手工冒烟 | 1～2 天 |

**合理估算：7～10 个有效开发日。** 该估算不包含微信集成、批处理或云端抽取。

排期受压时的裁剪顺序：先裁 §5.4 离线段（整段裁，不裁一半）；两个阻断项（gate 前移、全路径不落原文）不可裁。

如果 V1 就要求"只回复'这是人员'也能继续"并加入可重启的加密 pending clarification 状态，需再增加约 2～3 天（整体 9～13 天）。**不建议**把这部分塞进当前 V1，理由见 §15。

## 12. 验收标准

### 12.1 路由与云端隔离

- 粘贴一条匿名案件消息，路由为 `job-case`；粘贴一条匿名人员消息，路由为 `candidate`。
- intake 回合（含 `ambiguous-sensitive`、`multiple`、失败路径）的 `plan()`、`stream()`、`streamAnswer()` 调用次数均为零；云端分段车道是唯一且独立的云端调用，其输入是脱敏行投影。
- 多案件广播经车道分段后逐段本地导入（每段独立 ActionRun 与去重）；车道被拦截或返回 `unusable` 时回落本地提示，原文回填输入框。
- 车道输出的任何协议违规（越界行号、重叠范围、多余字段、超过 10 条）导致整体拒绝并回落，不做修复性解析。
- "最近有什么 Java 案件？"路由为 `not-intake`，继续走现有搜索。
- "明天10点约田中面试"路由为 `not-intake`，预约重定向链路照常工作（裸姓名不触发 fail-closed）。
- 含电话号码或邮箱但结构不足的文本路由为 `ambiguous-sensitive`，不进入 Planner。

### 12.2 案件

- 只生成一个 Job Case Review，不直接生成正式案件。
- 案件名称、工作内容、必须技能、地点、工作方式、单价等可识别字段进入正确位置。
- 价格行中的"根据技能和经验面议"不会进入必须技能。
- 案件中包含"外国籍不可、日本語N2以上、年齢50歳まで"仍分类为案件，不返回 `multiple`。
- HR 修改并确认后，正式案件可在现有案件页面查看。
- 重复提交按 §10.2 生命周期表处理，不新增草稿。

### 12.3 人员

- 人员经历含"要件定義"不误判为案件。
- 只生成一个 Candidate Review，不直接进入正式人才池。
- 姓名或首字母、技能、经验年数、单金、开始日、日语、最近车站和工作希望按可用信息提取。
- 没有法定姓名时以首字母作为待审核显示名，不虚构姓名。
- 没有明确项目结构时，不产生虚假的多段项目经历。
- HR 修改并确认后，候选人档案可在现有候选人页面查看。
- 重复提交按 §10.2 处理，不新增候选人草稿或源文件。

### 12.4 标注、边界与隐私

- 首行 `【人员】` 使结构足够但根证据不足的文本按人员解析；`【案件】` 同理。
- 首行标注与正文决定性结构矛盾时返回 `ambiguous-sensitive` 并说明矛盾。
- 标注不能覆盖 `multiple`。
- `ambiguous-sensitive` / `multiple` 时不写入草稿，原文回填输入框且不落盘。
- 任何 intake 结果路径（成功、重复、ambiguous、multiple、解析失败、保存失败）的持久化会话中都不包含原文，只有安全摘要。
- 原始身份信息不出现在 ActionRun、云端请求、应用日志或测试快照中。
- 应用重启后，待审核草稿仍可继续处理；会话中只有安全摘要。
- 删除候选人及来源后，对应加密文本和映射均被清理。
- （若离线段未裁）Cloud 未连接时聊天框可用，本地导入工作，`not-intake` 返回确定性本地提示；本地导入期间不显示模型名称、规划进度或远端取消/计费提示。

## 13. 测试与 fixture 清单

fixture 必须来自真实样本的匿名化变体，至少固定以下回归样本：

1. 案件中包含"外国籍不可、日本語N2以上、年齢50歳まで"，仍分类为 `job-case`。
2. 人员项目经历中包含"要件定義"，仍分类为 `candidate`。
3. 人员消息没有法定姓名，只有首字母和人员标题，分类为 `candidate` 并进入审核。
4. 同一条消息有两组 `氏名` 区块，返回 `multiple`。
5. 同一条消息有完整案件区块和完整人员区块，返回 `multiple`。
5a. 月度案件广播（多行 `案件1️⃣：` / `案件2️⃣：` 编号简写、混合中日文、无冒号的月份小节标题）返回 `multiple`，不落入 `ambiguous-sensitive`。
6. "最近有 Java 案件吗？"返回 `not-intake`，进入现有 Planner。
7. "明天10点约田中面试"返回 `not-intake`，且预约链路可达（防止裸姓名误伤的反向用例）。
8. 有手机号或邮箱但结构不足的文本返回 `ambiguous-sensitive`，不进入 Planner。
9. 命中根标签但字段不足的文本返回 `ambiguous-sensitive`。
10. 首行 `【人员】` + 决定性案件结构，返回 `ambiguous-sensitive`（矛盾说明）。
11. 首行 `【人员】` + 两组人员记录，返回 `multiple`（标注不覆盖 multiple）。
12. **每个 intake fixture 断言 `narrativeStreamer` 的 `plan` / `stream` / `streamAnswer` 调用次数为零。** 测试不能只验证生成了草稿，还要证明敏感文本根本没有经过 Planner 路径。
13. **每个 intake 结果路径断言持久化会话不含原文**：成功、重复、`ambiguous-sensitive`、`multiple`、解析失败、保存失败六条路径逐一覆盖。
14. 云端车道协议 fixture：合法多段响应（含 fenced JSON）、`unusable`、越界/重叠/逆序行号、未知 kind、空记录、超过 10 条——违规一律拒绝。
15. 云端车道行为 fixture：多案件广播 → 分段本地导入且每段文本与行号切片一致；单段 → 与本地路径相同的导航块；DLP 拦截 / `unusable` → 回落提示；全部分段失败 → 回合失败、会话无原文、原文回填。

实现后至少完成：

- TypeScript 类型检查（含 `rawSourceText` / `persistedUserContent` 类型隔离的编译期验证）。
- Tool catalog、参数约束、权限声明和 Action runtime 测试。
- 分类器（归一化、根标签、区块、fail-closed 边界）、案件段落解析和候选人标签解析单元测试。
- 持久化与历史 Schema 升级测试（含 `CandidateSourceFormat` 契约拆分波及的 row mapper、预览、删除测试）。
- 隐私投影、日志和会话历史测试。
- Renderer 输入长度、回填、离线与 Review Center 导航测试。
- macOS 开发包安装和手工冒烟测试。

测试数据只使用匿名化 fixture。真实微信样本仅在本机进行手工对照，不提交 Git。

## 14. 失败处理与回滚

- 分类失败：按 `ambiguous-sensitive` 处理（fail closed），不创建草稿，不落原文。
- 解析失败：保留可重试的失败状态和安全错误码，不在错误中回显原文，不落原文。
- 草稿保存失败：不返回成功，不产生部分正式数据，清理残留暂存文件。
- Review Center 确认失败：草稿保持待审核，允许 HR 重试。
- **回滚开关**：两层独立。`SES_BUSINESS_TEXT_CLOUD_ASSIST_ENABLED=0` 只关云端分段车道，`ambiguous-sensitive` / `multiple` 恢复纯本地提示；`SES_BUSINESS_TEXT_INTAKE_ENABLED=0` 关整个 gate，所有消息路由为 `not-intake`，行为完全回到基线。现有案件、候选人和聊天查询能力不受影响。
- 已生成的待审核草稿仍由 HR 手动确认或删除，不自动转正。

## 15. 后续扩展停止条件

V1 上线并稳定之前，不扩展成通用业务文本平台。

- ~~批量导入的评估条件~~（v0.3 已满足并实施：真实样本证明月度广播即多记录消息，云端分段车道提供受控批量，人工逐条确认不变）。
- 在 4,000 字限制确实阻挡单条完整业务消息时，才评估提高上限。
- ~~云端提取的评估条件~~（v0.4 实施：真实速记体消息（「② 8月～長期、5名，VC++3年以上…」）使本地标签解析器只能得到标题，触发了 v0.3 预留的条件；字段级云端提取按预留方式启用——仅接收脱敏投影、字段值逐字核验、占位符本机还原）。
- 只有案件和候选人拥有稳定、可验证的业务 ID 与状态规则后，才评估自动状态更新。
- **澄清体验（V1.1 预留）**：只有真实使用证明"整段重发"成本确实高时，才实现免重发澄清。届时也不泛化成"扫描会话历史"，而是增加一个显式绑定的 pending token：

  ```text
  conversationId
  sourceHash
  encryptedSourceToken
  createdAt
  expiresAt
  expectedClarificationType
  ```

  Tool 作用域届时才扩展为 `current-agent-message | explicitly-bound-pending-intake`，而不是笼统的"可以读取上一条消息"。

这些条件未满足时，保持"一次一条、一个本地 Tool、一个待审核草稿"的方案不变。

## 16. 结论

SES Agent 业务文本接收 V1 不建设新的导入平台，也不给云端规划模型增加任何选择权。它只在现有 Agent 入口之前补齐一层本地确定性路由：

```text
HR 手动粘贴一条微信业务消息
  -> Main 本地 intake gate（先于任何云端调用）
  -> 命中：一个案件草稿或一个人员草稿（整回合零云端调用、会话不落原文）
  -> Review Center 人工核对
  -> 确认后进入现有案件库或候选人库
  未命中（not-intake）：进入现有 Cloud Planner，一切照旧
```

该方案覆盖当前确认的真实使用方式，同时保留现有审核、隐私、去重和删除边界，并额外获得两个性质：敏感业务文本从始至终不进入云端投影；本地导入不依赖云端可用性。V1 的成功标准不是"能理解任意聊天记录"，而是"能稳定、安全地处理一条明确的案件或人员消息，并且永远不因此把原文送上云端或留在会话里"。
