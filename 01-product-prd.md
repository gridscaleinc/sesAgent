# SES Agent Desktop 产品 PRD

> 版本：v0.6
> 日期：2026-07-17
> 产品阶段：内部桌面 MVP

## 1. 产品摘要

### 1.1 一句话定义

让 SES 营业员用一条受控任务发起“读取资料—整理案件—匹配候选人—生成提案”，并在每个关键节点审核 AI 的结果。

### 1.2 产品定位

SES Agent Desktop 是一个本地优先的 SES 营业与招聘资源工作台。它提供类似 WorkBuddy 的持续任务上下文，但自然语言只能路由到明确的 SES 领域用例，不是通用聊天机器人、任意工具 Agent 或完整 HR 系统。

其核心价值是缩短“收到案件邮件”到“形成可发送提案”的时间，同时保留人工审核和业务责任边界。

平台顺序已经确认：先完成 macOS 安全试点，Go 后立即开发 Windows 同功能版本。首版邮箱已经确认为 Google Workspace，只实现 Gmail API，不同时建设 Microsoft 365 适配器。

### 1.3 为什么先做桌面应用

- 不需要先建设服务器、账号体系和多租户后台。
- 原始简历、解析缓存和业务数据优先保存在本机。
- 可以直接读取营业员选中的本地文件，并与现有邮箱工作方式结合。
- 能够以较低的部署成本快速进入真实试点。
- 产品验证成功后，桌面端仍可作为团队版与 SaaS 的高效率操作客户端。

## 2. 业务问题

SES 营业员日常存在以下问题：

1. BP 案件邮件数量大、格式不统一，优质案件具有明显时效性。
2. 技能表主要以 Excel、PDF 和 Word 流转，字段位置与模板不统一。
3. 候选人技能、日语、单价、稼动时间和现场条件分散在文件与个人记忆中。
4. 匹配依赖营业员经验，遗漏和重复劳动多，新人上手慢。
5. 提案邮件和脱敏附件需要重复制作，但错误会直接造成业务与隐私风险。
6. 现有通用 CRM 记录能力强，但无法理解 SES 特有的技术与商流语义。

## 3. 产品目标与非目标

### 3.1 MVP 目标

- 关键简历字段解析准确率达到 90% 以上。
- 案件邮件关键字段解析准确率达到 85% 以上。
- 从打开案件到形成可审核提案草稿的中位时间低于 3 分钟。
- 单个营业员每天可审核的有效案件数提升至少 3 倍。
- 所有外发内容都经过明确的人工确认。
- 在不部署业务服务器的前提下完成真实数据试点。
- 用户能够中断、继续和恢复一项业务任务，不需要重新描述数据范围和已确认条件。
- 任何提交到云端模型的载荷都不包含能够直接定位个人的姓名、电话、私人邮箱、住址、生日、照片、证件号等信息。
- 隐私关键预处理和适合的小模型任务优先在本地执行，云端只处理通过脱敏与 DLP 校验的复杂语义任务。

### 3.2 非目标

- 不替代营业员维护客户关系、商务判断和最终提案责任。
- 不自动作出录用、拒绝或人员优先级的最终决定。
- 不在 MVP 阶段实现多人实时编辑。
- 不实现工资、法定考勤、社保、税务和绩效管理。
- 不承诺理解所有损坏文件、加密文件和手写扫描件。

## 4. 目标用户

### 4.1 SES 营业员

核心任务：快速发现有价值的案件、找到合适人选、生成提案并跟进。

主要痛点：邮件过载、候选人信息分散、提案速度慢、依赖个人记忆。

MVP 中它是唯一主操作角色。试点不承诺跨设备任务移交、共享候选人库或多人审批。

### 4.2 招聘/人事人员

核心任务：接收并维护候选人技能表，确保个人资料准确、可追溯。

主要痛点：简历模板不统一、重复录入、版本混乱、缺少结构化检索。

MVP 中 HR 是业务上游：通过文件、共享盘导出或现有邮件向主操作人提供资料，不作为第二个同步登录角色。

### 4.3 营业负责人

核心任务：了解案件处理效率、解析错误、提案质量和团队使用价值。

MVP 只提供主操作人本机汇总与导出报告；负责人独立登录、集中审批和团队级看板在协作版实现。

## 5. 核心用户旅程

### 5.1 首次配置

1. 用户安装并打开应用。
2. 设置本地数据目录与备份策略。
3. 首发不要求配置云端大模型凭据，Cloud Gateway 默认关闭；未来只有在 Provider、用途和数据区域独立批准后，才允许把凭据写入操作系统密钥链。
4. 应用检查本地 OCR、PII 检测、Embedding 模型和设备资源，并展示哪些能力完全离线运行。
5. 连接 Google Workspace；暂不授权 Gmail 时也可以只使用简历库和手工案件入口。
6. 应用执行连接测试，并展示会读取、在本地处理和保存在本地的数据范围；首发同时明确显示 Cloud Provider 未启用，不能把“未来可发送脱敏载荷”表述成当前已联网调用。

### 5.2 发起与继续业务任务

1. 用户从全局任务输入器、案件详情或候选人详情发起一项工作。
2. 系统将输入路由为受支持的任务类型：导入简历、创建案件、匹配候选人或生成提案。
3. 执行前展示任务预览：任务类型、将读取的数据源、数据策略、计划步骤与必需的人工确认点。
4. 缺少费率、稼动时间、日语等级或发送对象时，系统进入 `awaiting_input`，并以结构化字段请用户补充。
5. 用户可以中断、取消、继续或重试任务；后续输入默认继承该任务已绑定的数据范围和已确认条件。
6. 任务页持续展示执行步骤、产物、来源证据、待确认事项和审计摘要。

> 当前实现（v0.35）：首页和任意业务页面可用 `⌘/Ctrl+K` 打开本地命令面板。命令只导航或调用已有的文件选择、手工案件、Gmail 状态动作与治理入口；搜索字符不持久化、不调用模型、不发送网络。侧栏“マイタスク”和首页“すべて見る”进入统一任务中心，侧栏“レビュー”进入任务/候选人/案件统一审核中心。任务详情把主结果、来源证据和执行管控分成真实的三个视图；证据只显示匿名候选人、来源标签、算法/策略/结果哈希、匿名成果物和 ToolAudit，管控明确显示固定 Scope、直接标识符 Cloud 禁止、Cloud 执行状态、审批门及安全重放策略。两个辅助视图从已加载快照派生，不触发新 IPC 或模型调用。侧栏操作员不再是硬编码示例账号：用户可配置只存 SQLCipher、禁止上云的本机显示名/角色，Electron Main 将稳定 Operator ID 绑定到新审核、反馈、删除和提案审计，过去记录保持不变；未配置时只显示“本机用户”。一次性 UI 请求在目标组件消费后立即清除，页面重挂载不会重放文件选择、案件窗口或 Google 设置。

未知或不支持的意图不自动获取更多工具权限，应要求用户选择已支持的业务操作或转为手工处理。

### 5.3 导入候选人简历

1. 用户拖入一个或多个 Excel、PDF 或 Word 文件。
2. 应用本地解析文件结构，建立可追溯的中间表示。
3. 本地 PII 引擎提取并隔离身份字段，将直接标识符替换为稳定占位符，再执行 DLP 复检。
4. 本地小模型先做可验证的基础分类/提取；需要云端复杂语义理解时，只提交通过校验的脱敏载荷。
5. 系统运行格式与逻辑校验，将不确定字段标记为待确认。
6. 用户在“原文/原表格—结构化字段”对照界面中修正并确认。
7. 候选人进入可检索资源池，原始文件形成版本记录。

### 5.4 创建与处理案件

1. 用户可以手工创建案件、粘贴邮件正文、导入 `.eml`，或从已连接邮箱导入。
2. 已连接邮箱时，应用运行期间按固定间隔同步新邮件。
3. 本地规则或本地分类模型先判断是否可能为案件、人员提案或无关邮件，并移除签名、历史收件人链和个人直接标识符。
4. 需要云端模型时，只有 `cloud-redaction-v2` 与独立 DLP 通过的脱敏正文可以用于提取技术要求、单价、税别、精算幅、支付条件、契约形态、商流、面谈次数、粗粒度地点、远程、语言、开始/结束时间和工时；国籍、在留资格与个人就劳资格一律先在本机替换为类型占位符。
5. 重复或转发的同一案件被合并，并保留来源记录。
6. 低置信度案件进入“待确认”，高置信度案件进入“可匹配”。

### 5.5 匹配并生成提案

1. 用户打开案件详情。
2. 系统先依据可用时间、工作授权、地点、单价等明确条件过滤。
3. 系统从技能与项目经历中召回候选人，并给出匹配与风险依据。
4. 用户选择候选人，必要时修改公开字段。
5. 系统生成日文提案邮件和脱敏附件预览；附件可包含 HR 已确认的项目经历，但不包含来源文件名、来源标签、内部 ID 或直接标识符。
6. 用户确认后导出提案包，并在应用外通过公司批准的邮件流程发送；当前 Google Workspace 集成保持 GET-only，不创建 Gmail 草稿。
7. MVP 不允许模型绕过用户直接发送邮件。

### 5.6 反馈闭环

用户可以将推荐标记为“合适”“不合适”并选择原因，例如技术不符、单价不符、稼动时间不符、客户偏好或资料过期。提案导出后由营业员按实际发生顺序手工记录“应用外发送完成、收到回复、面谈进行、参画决定、见送り、辞退”；导出本身不代表已发送，终态不能继续推进。这些记录只用于本地业务追踪，不直接改变原始简历、匹配排序或自动做出雇佣决定。

## 6. 信息架构

### 6.1 作业

- 新建受限业务任务
- 我的任务与最近任务
- 等待输入、执行中、等待审核、失败与已完成视图
- 持续消息、数据范围、执行步骤、产物、证据与审批门
- 中断、继续、取消、重试和归档

### 6.2 今日

- 新案件数量
- 待确认简历
- 待确认案件
- 最近生成的提案
- 解析失败与需要处理的任务

### 6.3 案件

- 今日案件
- 全部案件
- 重复案件组
- 案件详情与原始邮件
- 推荐候选人
- 提案历史
- 手工创建、粘贴邮件和 `.eml` 导入

### 6.4 候选人

- 候选人列表与组合筛选
- 技能、语言、单价、可用时间、地点筛选
- 候选人详情
- 简历版本与原始文件
- 历史匹配与用户反馈
- 资料最后确认时间、有效期与重新确认任务

### 6.5 审核中心

- 简历字段确认
- 案件字段确认
- 脱敏结果确认
- 失败任务重试

### 6.6 设置

- 邮箱连接
- 显示语言（日文 / 简体中文）
- 模型与 API 凭据
- 数据目录、备份与删除
- 提取字段与匹配权重
- 隐私和外部数据传输说明
- 应用更新与诊断信息

## 7. 功能需求

### 7.1 P0：必须交付

| 编号 | 功能 | 验收要点 |
|---|---|---|
| P0-01 | 批量导入简历 | 支持目标格式、显示进度、失败不影响其他文件 |
| P0-02 | 本地文档解析 | 保留工作表、页面、单元格或文本位置来源 |
| P0-03 | 结构化提取 | 输出固定 Schema，缺失字段为 null，不允许编造 |
| P0-04 | 人工校对 | 字段可编辑，并可跳转或定位到来源 |
| P0-05 | 候选人库 | 搜索、筛选、查看版本、归档和删除 |
| P0-06 | Gmail 增量同步 | Google Workspace OAuth；可暂停；从检查点补齐；不重复导入同一邮件 |
| P0-07 | 案件识别与提取 | 区分案件、人员提案和无关邮件 |
| P0-08 | 案件去重 | 基于 Message-ID、线程和业务特征识别重复 |
| P0-09 | Hybrid RAG 候选人匹配 | 硬过滤、BM25、向量召回、AI 精排、来源解释和风险提示 |
| P0-10 | 提案草稿 | 生成日文邮件与脱敏附件预览 |
| P0-11 | 人工发送门 | 未确认时不能执行任何外发动作 |
| P0-12 | 本地安全 | 密钥链、敏感字段加密、数据导出、活动数据删除和备份过期/密钥销毁 |
| P0-13 | 任务恢复 | 应用崩溃或退出后可以识别并恢复未完成任务 |
| P0-14 | 用户作业任务 | 持久化任务消息、上下文、步骤、产物和等待输入/审核状态 |
| P0-15 | 受限意图路由 | 只能路由到导入、建案、匹配和提案用例；不支持的意图不执行工具 |
| P0-16 | 任务预览与数据范围 | 执行前显示任务类型、数据源、策略、计划和必需确认点 |
| P0-17 | 手工案件入口 | 邮箱未连接时仍能手工录入、粘贴邮件或导入 `.eml` |
| P0-18 | 提案生命周期 | 区分草稿、已确认、已导出未发送和人工追加的发送/回复/面谈/最终结果；当前不创建 Gmail 草稿 |
| P0-19 | 删除报告 | 显示主记录、原文件、派生文本、索引、缓存和备份的处理状态 |
| P0-20 | 云端强制脱敏门 | 所有 Cloud AI 调用只能接收通过本地 PII 检测与 DLP 复检的脱敏载荷；用户不能绕过 |
| P0-21 | 本地 AI 基础层 | 在无网络本地进程完成 OCR、PII 检测、DLP、Embedding 和基础分类，并验证模型哈希与资源上限 |
| P0-22 | 显示语言 | 设置中可在日文与简体中文间立即切换；偏好仅本地加密保存，业务数据不被自动翻译或上传 |

> 实现状态（2026-07-20）：P0-13/P0-14 的用户可见任务记录已进入主链。消息、上下文、步骤、审批门、匿名产物和 ToolAudit 随 WorkTask 加密持久化；候选检索、简历确认和提案生命周期追加结构化记录。Schema v22 的独立 `ProcessingJob` 已用于候选人匹配、简历分析和提案导出，具备幂等请求指纹、租约、进度、指数退避、最大试行次数、取消请求、结果哈希和崩溃恢复；同范围重试不会重复创建 Match Run、用户消息、成果物或审批。候选匹配/简历分析为 `safe-local`，由 Electron Main 后台 Dispatcher 在启动和到期时主动取队；提案导出为不可自动重放的 `manual-review`。本地 AI 与文件导出已有独立 FIFO 并发车道；将 Dispatcher 进一步隔离到独立 Utility Process 仍是加固项，不是当前恢复链的前置条件。

> P0-18 实现状态（v0.35）：Schema v27 以 append-only 事件保存提案后的人工跟进。第一条必须是“应用外发送完成”，之后才能追加回复、面谈或最终结果；日期不能倒退，终态不能继续推进，旧窗口由乐观 Revision 拒绝。第一条事件写入后冻结提案正文、附件、批准和再次导出，避免业务结果与内容版本脱钩。记录固定 `cloudEligible=false`，不调用 AI、不写 Google Workspace；备注命中电话、邮箱等直接标识符时拒绝保存，操作员由 Electron Main 从本机档案绑定。

> P0-22 实现状态（v0.36）：侧栏新增“显示与语言”入口，提供日文与简体中文单选。选择立即切换当前界面、`html lang` 和中文字体栈，且能随 SQLCipher Schema v28、加密快照和恢复包保留；切换只调用本地 Electron Main IPC，使用乐观 Revision，不重新拉取 Bootstrap，不调用 AI、Gmail 或网络。翻译层只处理显式允许的界面文案，任务指令、候选人/案件数据、原文、备注和审计以其原始语言显示，防止 UI 切换改写业务记录。

> Google Workspace 预检状态：应用内可在授权前验证本机 Loopback、OAuth/Gmail 公网到达、OS Token 保护及有界同步配置，并明确证明诊断未访问邮箱、未创建凭据。首页主输入区已提供可执行的 Gmail 状态入口：未配置直接进入管理设置，未连接进入只读授权说明，已连接执行冻结范围同步并把结果带入案件审核。连接后在线受入检验实时复验 Gmail Profile、唯一只读 Scope 与公司域，再用 Schema v23 中的最近同步和逐条脱敏证据形成最小化报告；报告不保存邮箱、Token、正文、Label 或关键词。Google Cloud 项目内的 Gmail API 开关、OAuth Client 实际类型和 Internal Consent Screen 无法由公开端点证明，继续作为管理员人工门；真实公司账号执行仍需公司 Client ID。

### 7.2 P1：macOS 试点后与 Windows 版本

> 实现状态（2026-07-20）：Windows 共用 OCR/DocumentIR/脱敏审核合同、DPAPI、x64 NSIS、Win32 ONNX/AVX2 Reranker、目标机包验证器和跨平台恢复已进入代码；固定 Tesseract WASM 日英离线 OCR Worker 已完成扫描 PDF 功能回归和资源完整性校验。OCR、Parser、Embedding、Reranker 已接入零网络 Capability 的 AppContainer 原生启动器，并有 Windows x64 目标机功能/Loopback 拒绝证据生成器；缺少启动器时本地 Worker 不回退到普通进程。由于本机不是 Windows，内核证据尚未取得，OCR Runtime 仍显示「隔離検証待ち」并 fail-closed。Windows 实机包、代码签名及安装/升级/卸载/恢复实测尚未完成，不能按“同功能版本”验收。

- 多邮箱或共享邮箱支持。
- 面试候选时间建议与日历草稿。
- 合同到期和候选人资料过期提醒。
- 常用提案模板与公司术语表。
- Windows 同功能版本：平台 OCR、系统密钥、原生依赖、代码签名、MSIX/安装包、升级与恢复验证。
- 可选的应用开机自启和后台驻留。
- 批量导出营业日报。

### 7.3 P2：团队协作版

- 公司工作区、账号与角色权限。
- 加密同步候选人、案件、反馈和提案状态。
- 冲突处理与审计日志。
- 团队规则、模板和模型配置集中管理。
- 管理者效率与质量看板。
- 许可证、订阅和多租户隔离。

## 8. 核心业务规则

### 8.1 数据状态

候选人资料状态：

```text
导入中 → 待确认 → 可匹配 → 已归档
          ↓
        解析失败
```

案件状态：

```text
新邮件 → 待确认/可匹配 → 已生成提案 → 已处理
            ↓
          无关/重复
```

用户作业任务状态：

```text
草稿 → 等待输入 → 已计划 → 执行中 → 等待审核 → 已完成
                        ↓          ↓             ↓
                      失败      已取消         已归档
```

提案状态：

```text
生成中 → 待确认 → 已确认 → 已导出（未发送） → 人工确认已发送
                         ↓                      ↓
                 已失效（内容变更）       已回复 → 面谈进行
                                                   ↓
                                      参画决定 / 见送り / 辞退
```

“已导出”不等于“已发送”。当前 Google Workspace 只读集成不写草稿、不发送、不自动推断结果；发送、回复、面谈和最终结果只能由用户明确记录。跟进开始后冻结提案内容，防止时间线指向后来修改的正文或附件。

### 8.2 匹配责任边界

- 系统提供排序建议，不作出录用或拒绝决定。
- 国籍等敏感属性不能作为自动歧视条件；案件中的“外国籍不可/日本国籍限定”等条件直接阻断。只允许处理具有明确业务与合规依据的法定就劳资格分类，并要求人工确认。
- 匹配理由必须引用候选人资料和案件中的具体证据。
- 缺失信息必须显示“未知”，不能当作“不满足”。
- 已过期的技能表必须明显提示，不应静默参与高置信度推荐。

### 8.3 外发规则

- 原始简历默认不能直接作为营业附件发送。
- 必须生成脱敏副本并展示差异预览。
- 所有收件人、主题、正文和附件都必须在一个确认页展示。
- MVP 默认只创建草稿或打开现有邮件客户端，不提供无人值守发送。

### 8.4 任务交互与工具边界

- 用户的自然语言输入先转换为受支持的 `WorkTaskType`，再调用明确领域 API。
- MVP 允许的任务类型只包含 `IMPORT_RESUME`、`CREATE_CASE`、`MATCH_CANDIDATES` 和 `GENERATE_PROPOSAL`。
- 模型可以提取意图和缺失字段，但不能自由选择工具、扩大数据范围或修改审批要求。
- 每个任务记录计划的用例、允许原因、实际执行、产生的证据、阻止原因和所需人工确认。
- 用户后续输入默认只能使用该任务已绑定的数据范围；扩大范围时必须重新预览和确认。
- 任务可以中断或取消，但不能回滚已经被用户导出到应用外的文件；这类副作用必须在确认前说明。

### 8.5 RAG 功能边界

> 实现状态（2026-07-20）：已完成经验、费率上限、稼动时点、远程频度、日语等级、粗粒度希望勤務地与法定就劳资格的 `tri-state-v3` 硬条件、本地 BM25、Profile/ProjectExperience 独立多语 Embedding、SQLCipher 分段缓存、候选人级聚合、RRF 与固定日文 Cross-Encoder 本地精排 Stage 4；未知字段保留并显示 HR 确认风险，不当作不满足。界面展示 BM25/Vector/RRF 前序/精排/最终 Rank、硬条件证据与命中项目来源，不把余弦、融合分数或精排 Logit 显示为录用概率。Schema v24 保留旧 `tri-state-v2` 并支持当前 `tri-state-v3`，Schema v25 保留历史 BM25/RRF Run 并记录本地精排策略；Schema v20 增加应用内 HR 专家标注，要求确认完整 Active 候选人母集，并在端末内计算 Recall@20/NDCG@20/Project Evidence。P0-09 技术能力已实现，真实 30–50 件 SES 专家标注集仍是试点发布门槛。

MVP 包含面向 SES 撮合的 Hybrid RAG，不是通用知识库聊天：

```text
案件字段构造检索 Query
        ↓
结构化硬过滤（时间、费率、地点、语言、工作授权）
        ↓
BM25/关键词召回 + Profile/项目经历向量召回
        ↓
候选人级聚合、RRF 并形成 Top 20
        ↓
端末内日文 Cross-Encoder 精排；字段/项目证据确定性解释
        ↓
营业员确认与反馈
```

RAG 知识源只包含已经人工确认、状态为“可匹配”的 CandidateProfile 和项目经历。未确认简历、原始个人联系方式、已删除资料和过期索引不能进入召回上下文。

MVP 的 RAG 支持：

- 案件自动检索候选人。
- 用自然语言搜索候选人，例如“Java 5 年以上、AWS、有电商经验、8 月可稼动”。
- 展示匹配依据、冲突和缺失信息，并关联候选人及案件来源。
- 收集营业员对结果的接受/拒绝原因，用于调整检索与排序。

MVP 不包含对所有邮箱、合同和公司制度进行开放式问答。若后续需要“公司知识库问答”，应使用独立索引、权限和数据保留策略，不能与候选人 RAG 默认混用。

### 8.6 云端隐私与本地 AI 边界

- 姓名、电话、私人邮箱、住址、生日、照片、人脸、签名、证件号、个人 URL/账号和可直接定位个人的二维码属于强制脱敏字段。
- 本地解析、OCR、PII 检测、占位符映射和 DLP 复检可以处理原始数据，但运行进程必须无网络、无云端凭据且不能读取任务范围外文件。
- 占位符映射只在本地加密保存，例如 `<PERSON_001>`、`<PHONE_001>`；云端模型只看到占位符，结果返回后由本地按用途恢复。
- 用户同意、Provider 的“不训练”承诺或企业合同都不能绕过产品内的强制脱敏门。
- 图片或扫描 PDF 无法在本地可靠识别并遮盖直接标识符时，禁止提交云端视觉模型，应转本地 OCR、本地视觉模型或人工录入。
- 本地 AI 优先承担 OCR、PII/DLP、Embedding、任务分类、邮件初筛和首轮结构化提取；复杂日文语义与提案润色可使用云端，但输入仍必须脱敏。
- 本地模型不可用或质量不达标时必须提示用户或进入人工流程，不能静默把原始资料回退到云端。

## 9. 非功能需求

### 9.1 性能

- 应用冷启动目标低于 5 秒，不包含首次数据库迁移。
- 单份普通简历的本地解析目标低于 5 秒。
- 10 份简历可排队处理，界面保持可操作。
- 5,000 名候选人规模下，常规筛选响应低于 300 毫秒。
- 匹配结果总耗时主要受模型 API 影响，应展示阶段与预计状态。

### 9.2 可靠性

- 任务写入持久队列后再执行。
- 用户可见的 `WorkTask` 与内部 `ProcessingJob` 分离；单个后台任务重试不得重复创建用户消息、产物或审批。
- 每个文件、邮件和模型请求使用幂等键，重试不得产生重复记录。
- 数据库迁移前自动备份。
- 应用异常退出后不留下永久“处理中”状态。
- 启动恢复后，用户能继续之前的任务上下文，并能看到哪些步骤已经完成、正在重试或需要重新确认。

### 9.3 可解释性

- 每个 AI 提取字段尽可能关联来源位置。
- 匹配结果显示满足条件、风险、缺失信息和推荐依据。
- 用户能够看到原始内容、程序解析结果和最终结构化结果之间的差异。
- 每个用户任务能够说明“使用了哪些数据、调用了哪个领域用例、为什么允许、产生了什么证据”。

### 9.4 可维护性

- 文档解析、模型提供商、邮箱连接和存储通过接口隔离。
- 本地 AI 与云端 AI 使用不同接口类型；云端接口在类型和运行时都不能接受原始 `DocumentIR/MailContent`。
- Schema、Prompt 和规则必须带版本号。
- 核心评测集纳入自动化测试，升级模型或 Prompt 前后必须对比。

## 10. 成功指标

### 10.1 产品指标

| 指标 | MVP 门槛 | 理想目标 |
|---|---:|---:|
| 简历关键字段准确率 | ≥ 90% | ≥ 95% |
| 案件关键字段准确率 | ≥ 85% | ≥ 92% |
| Top 3 推荐获营业员认可率 | ≥ 60% | ≥ 75% |
| 案件到提案草稿中位时间 | < 3 分钟 | < 90 秒 |
| 需要人工修改的关键字段比例 | < 25% | < 10% |
| 异常退出后用户任务可恢复率 | 100% | 100% |
| 已导出提案的后续结果记录率 | ≥ 70% | ≥ 90% |
| 试点用户周活跃天数 | ≥ 3 天 | ≥ 5 天 |

### 10.2 安全指标

- 自动发送邮件次数为 0。
- 未经确认发送原始简历次数为 0。
- API 密钥出现在日志或数据库明文中的次数为 0。
- 云端模型请求中直接个人标识符检出次数为 0。
- 固定脱敏评测集对姓名、电话、邮箱、住址、生日、证件号和图片身份区域的漏检率必须达到发布阈值；任一漏检样本不得进入云端回放测试。
- 每次删除都产生机器可读报告，覆盖数据库记录、索引、缓存、受管原文件及备份过期/密钥销毁状态。

## 11. 产品发布门槛

只有同时满足以下条件才进入真实业务试点：

1. 50 份脱敏简历的关键字段准确率达到门槛。
2. 200 封脱敏邮件的分类和字段提取达到门槛。
3. 关键安全检查通过，日志中不存在简历全文或 API 密钥。
4. 所有 Cloud AI 入口只能接收 `dlpStatus=passed` 的脱敏载荷，原始文本、附件和图片的绕过测试全部被阻止。
5. 提案生成到外发之间存在不可绕过的人工确认步骤。
6. 可完成数据导出、加密备份、恢复、活动数据删除与备份过期/密钥销毁演练。
7. macOS 签名安装包由一名主操作营业员完成至少一周的受控试用，且一名 HR 或负责人以资料提供/结果观察者身份确认应用外交接可接受。

## 12. 已确认决策与开放问题

已确认：

- 首发 macOS；macOS 试点 Go 后开发 Windows 同功能版本。
- 公司邮箱为 Google Workspace；MVP 只实现 Gmail API。
- 安全试点首版只生成可导出提案包；Google Workspace 保持 `gmail.readonly` 与 GET-only，不创建草稿、不发送。

仍需确认：

- 公司当前候选人主数据在 Excel、Google Sheets、CRM 还是共享盘？
- 业务方如何定义“推荐正确”：愿意提案、通过书类审查，还是最终面试成功？
- 试点设备的最低 Apple Silicon、内存和可用磁盘配置是什么？本地小型 LLM 若不能达到日文提取评测门槛，首版只保留 OCR、PII、Embedding 和分类能力。
